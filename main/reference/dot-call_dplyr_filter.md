# `dplyr::filter` call

Create a
[`dplyr::filter`](https://dplyr.tidyverse.org/reference/filter.html)
call

## Usage

``` r
.call_dplyr_filter(x)
```

## Arguments

- x:

  (`list`) containing `variables` and `values`
